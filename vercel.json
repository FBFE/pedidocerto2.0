{
  "buildCommand": "./flutter/bin/flutter build web --release",
  "installCommand": "if [ -d flutter ]; then (cd flutter && git pull); else git clone --depth 1 https://github.com/flutter/flutter.git; fi && ./flutter/bin/flutter config --enable-web",
  "outputDirectory": "build/web",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
